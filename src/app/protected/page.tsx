const Protected = () => {
    return (
        <div className="flex w-full h-screen justify-center items-center font-bold text-5xl">
            <h1>Protected Page</h1>
        </div>
    );
};

export default Protected;